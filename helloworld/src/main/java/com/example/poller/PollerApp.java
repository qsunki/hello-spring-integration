package com.example.poller;

import org.springframework.boot.SpringApplication;

public class PollerApp {
    public static void main(String[] args) {
        SpringApplication.run(PollerConfig.class);
    }
}
